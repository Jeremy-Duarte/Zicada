[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
testpaths = apps

addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --create-db
    --reuse-db